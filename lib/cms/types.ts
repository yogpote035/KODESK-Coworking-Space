export type PricingRecord = { id: string; service_key: string; service_name: string; price: number | null; price_label: string | null; currency: string; billing_period: string | null; description: string | null; is_active: boolean; sort_order: number };
export type MediaRecord = { id: string; name: string; storage_path: string; public_url: string; alt_text: string | null; category: "hero" | "gallery" | "service" | "other"; gallery_category: "workspace" | "meeting-rooms" | "lounge-areas" | "amenities" | null; is_active: boolean; sort_order: number };
export type ServicePageDetail = { service_slug: string; cover_image_url: string | null; hero_image_url: string | null; gallery_image_urls: string[]; hero_title: string | null; subtitle: string | null; hero_description: string | null; overview_title: string | null; overview_body: string | null; benefits: string[]; audience_title: string | null; audience: string[]; features: Array<{ title: string; copy: string }>; cta_title: string | null; cta_body: string | null; cta_label: string | null; cta_url: string | null; faq_items: Array<{ question: string; answer: string }>; seo_title: string | null; seo_description: string | null };
export type PageSection = { page_key: string; section_key: string; content: Record<string, string>; is_active: boolean; sort_order: number };
export type ContactSettings = { phone?: string; email?: string; address?: string; whatsapp_number?: string; whatsapp_message?: string };
export type SiteSettings = { contact_information?: ContactSettings; business_hours?: { reception: string }; social_links?: Record<string, string>; branding?: { site_name?: string; logo_url?: string; favicon_url?: string; default_og_image_url?: string; default_cta_label?: string; default_cta_url?: string } };
