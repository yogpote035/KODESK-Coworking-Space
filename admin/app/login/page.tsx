"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "../../lib/supabase";
import { Icon } from "../../components/admin/icons";
export default function LoginPage() { const router = useRouter(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (!supabase) return setError("Supabase is not configured for this admin app."); setLoading(true); setError(""); const { error: authError } = await supabase.auth.signInWithPassword({ email: email.trim(), password }); setLoading(false); if (authError) return setError("Unable to sign in. Check your email and password."); router.replace("/dashboard"); router.refresh(); }; return <main className="login-page"><section className="login-brand"><div><span className="brand-mark">K</span><b>KODESK</b></div><article><p className="section-kicker">WORKSPACE MANAGEMENT</p><h1>Run your workspace with clarity.</h1><p>One refined place for enquiries, pricing, visual content, and essential KODESK operations.</p></article><span className="login-orb one"/><span className="login-orb two"/></section><section className="login-form-wrap"><form className="login-card" onSubmit={submit}><p className="section-kicker">WELCOME BACK</p><h1>Sign in to KODESK</h1><p>Use your authorized administrator account.</p>{error && <div className="inline-error" role="alert">{error}</div>}<label>Email address<input required type="email" autoComplete="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@kodesk.com"/></label><label>Password<span className="password-field"><input required type={showPassword ? "text" : "password"} autoComplete="current-password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password"/><button type="button" onClick={() => setShowPassword(!showPassword)}>{showPassword ? "Hide" : "Show"}</button></span></label><button className="button button-primary login-submit" disabled={loading}>{loading ? "Signing in…" : <>Sign in <Icon name="arrow"/></>}</button></form></section></main>; }
